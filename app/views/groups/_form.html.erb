<%= form_for @group do |f| %>
<% if @group.errors.any? %>
<ul>
  <% @group.errors.full_message.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
</ul>
<% end %>
標題
<%= f.text_filed :title %>
<br>
<%= f.text_area :deasription %>
<br>

<%= f.submit "Submit", :disable_with => 'Submiting...'%>
<% end %>
